# HANDOFF - קשר של תפילין

מסמך מסירה לצ'אט/מפתח שלא מכיר את הפרויקט. נכתב 9.8.2026.
**קרא את הכל לפני שאתה נוגע בקוד** - יש כאן מלכודות שעלו בדם.

---

## 1. מה זה הפרויקט

אתר של מיזם **'קשר של תפילין'** של **עמותת אור חדש** (ע"ר 580703965). המיזם מחבר בין
אנשים שברשותם תפילין שאינן בשימוש לבין אנשים שרוצים להתחיל להניח.

האתר נולד כ**שכפול 1:1 של אתר וורדפרס** (`tefilin.or-hadash.org.il`) ולאחרונה הפך
ל**אתר מנוהל** עם מסד נתונים ומערכת ניהול תוכן.

| | |
|---|---|
| אתר חי | https://or-hadash.org.il |
| אדמין | https://or-hadash.org.il/admin |
| פרויקט Lovable | `167f27e2-2714-4390-92ec-a8678508fc95` |
| ריפו | `naomidigital101-ship-it/accurate-web-cloner` (ענף `main`) |
| Supabase | `ndrsficegioyptrbbuxu.supabase.co` (Lovable Cloud) |
| **הדומיין הישן** | `tefilin.or-hadash.org.il` - מצביע לאתר הזה ומחזיר 301/410 בלבד. ראה §7 |

**סטאק:** TanStack Start (SSR) + React + Supabase. עברית RTL, אנגלית תחת `/en`.

---

## 2. כללי עבודה - חובה

### 🔴 אסור לשלוח הודעות לצ'אט של Lovable (`send_message`)
זה כלל מוחלט של בעלת הפרויקט. **כל שינוי קוד עובר דרך גיט בלבד.**
שינויי נתונים ב-DB דרך SQL מותרים, וקריאות מ-Lovable MCP מותרות.

### תהליך עבודה תקין
```bash
gh repo clone naomidigital101-ship-it/accurate-web-cloner
bun install
# עורכים קוד
bun run build            # חייב לעבור לפני פוש
git commit && git push origin main
# ואז deploy_project דרך Lovable MCP
```

### פריסה
`deploy_project` (Lovable MCP). לוקח **40-90 שניות**. לפעמים צריך קריאה שנייה אם
Lovable עדיין סנכרן מגיט. הכלי נחסם מדי פעם ע"י מסווג הרשאות - להמתין ולנסות שוב.

---

## 3. ארכיטקטורה - הדבר החשוב ביותר להבין

### Lovable Cloud לא מחבר את Supabase לריפו
אין `supabase/` בריפו, אין `.env`, אין client מוכן. **אבל** המשתנים מוזרקים
**בזמן ריצה, בצד השרת בלבד**:

```
SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_PROJECT_ID
```

**אין משתני `VITE_`** → הדפדפן לא יכול לגשת ל-DB ישירות.

### מכאן נגזר הכל
- **כל גישה לנתונים דרך `createServerFn`** (`src/lib/api/*.functions.ts`)
- `src/lib/supabase.server.ts` - `publicDb()` (כפוף ל-RLS), `adminDb()` (service role), `requireStaff()`
- הדפדפן מקבל URL+publishable key **בזמן ריצה** דרך server function, לצורך auth בלבד
- תוכן נטען ב-`loader` של הראוט → **SSR** → גוגל ומנועי AI רואים תוכן מלא

### ⚠️ אי אפשר להריץ server functions מקומית
אין משתני DB במחשב → `bun run dev` יחזיר 500 על כל קריאת DB.
**זה תקין ומכוון:** כל הרכיבים נופלים חזרה למערכים שבקוד, והאתר מרונדר בדיוק כמו קודם.
**בדיקות של DB חייבות לרוץ מול פרודקשן.**

---

## 4. מסד הנתונים

12 טבלאות. RLS מופעל על כולן.

| טבלה | תוכן | דו-לשוני |
|---|---|---|
| `stories` | 43 עברית + 16 אנגלית | ✅ |
| `rabbi_letters` | 9 + 9 | ✅ |
| `press_items` | 9 + 4 | ✅ |
| `faqs` | 8 + 8 | ✅ |
| `services` | 3 + 2 | ✅ |
| `gallery_images` | 28 | משותף |
| `certificates` | 0 (ממתין לקבצים) | משותף |
| `site_settings` | 22 מפתחות | משותף |
| `media` | ספריית קבצים | משותף |
| `leads` | פניות מהטפסים | - |
| `user_roles` | הרשאות | - |
| `admin_allowlist` | מי רשאי להירשם | - |

### RLS
```
anon           → SELECT רק status='published'
anon           → INSERT בלבד ל-leads (לשלוח טופס), אסור לו לקרוא אותם
authenticated  → הכל, דרך is_staff()
מחיקה          → admin בלבד (נאכף ב-deleteRow)
```

### ⚠️ `has_role()` ו-`is_staff()` הן SECURITY DEFINER
בלי זה מדיניות RLS על `user_roles` שואלת את `user_roles` דרך RLS → **רקורסיה אינסופית**.
זו התקלה הכי נפוצה בהקמות Supabase. אל תשנה אותן ל-INVOKER.

### הרשאות בלי סיסמאות
טבלת `admin_allowlist` + טריגר `on_auth_user_created_grant_role` על `auth.users`:
מי שנרשם עם מייל מהרשימה מקבל תפקיד **מיד**. מי שלא ברשימה נרשם בלי תפקיד ולא רואה כלום.

**הוספת משתמש:**
```sql
insert into public.admin_allowlist (email, role) values ('someone@example.com', 'admin');
```
**אל תיצור סיסמאות עבור אנשים.** הם נרשמים בעצמם דרך `/admin`.

**רשומים כעת:** `naomi.digital101@gmail.com` (admin).
**חסר:** המייל של הלקוח (הרב עמיחי איל) - שאל את נעמי.

---

## 5. Field binding - איך התוכן מגיע לעמוד

**דפוס קבוע בכל מקום: DB עם נפילה חזרה לקוד.**

```ts
const list = fromDb && fromDb.length > 0 ? fromDb.map(...) : codeArray;
```

`src/lib/settings.tsx` מכיל `SETTING_DEFAULTS` - **בדיוק הערכים שהיו קשיחים בקוד**.
`useSetting()` נופל אליהם כשאין ערך.

**זו לא הגנה תיאורטית:** אם ה-DB נופל, האתר ממשיך לרנדר כרגיל. אל תסיר את הנפילות.

**קבצים:** `src/lib/content.ts` (קריאות), `src/lib/settings.tsx` (הגדרות),
`src/lib/api/content.functions.ts` (CRUD).

---

## 6. מלכודות שעלו בדם - קרא לפני שאתה נוגע

### 🔴 רכיב משותף לשני ראוטים לא יכול לקרוא `useLoaderData`
`EnPage` מרונדר גם ב-`/en/` וגם ב-`/en/the-tefillin-tie-initiative` (הכתובת הקנונית!).
הוספת `Route.useLoaderData()` בתוכו **רוקנה את העמוד הקנוני מ-104KB ל-8KB.**
הפתרון: הרכיב מקבל `data` כ-prop, כל ראוט מזרים את שלו, לואודר משותף מיוצא.

### 🔴 טפסים רב-שלביים מסירים שדות מה-DOM
`FormData` בשליחה מכיל **רק את השלב האחרון**. יש `useMultiStepValues()` שמצלם ערכים
בכל מעבר. אם תיגע בטפסים - אל תסיר את זה, אחרת יאבדו שם, כתובת ויד כותבת.

### 🔴 שתי רשימות שאלות נפוצות
`FaqSection` מכיל 8 שאלות ארוכות (מה שנראה באתר). `index.tsx` מכיל 4 קצרות שמזינות
רק JSON-LD. **המקור הנכון הוא FaqSection.** זריעה עיוורת מהקצרה מוחקת את התשובות האמיתיות.

### 🟡 10 סיפורים בעברית ריקים לגמרי
`paragraphs: []` - **גם בקוד המקור.** לא אובדן הגירה. אם הלקוח שואל - התוכן פשוט
מעולם לא נכתב עבורם.

### 🟡 מדידות בדפדפן בטאב מוסתר
`visibilityState:hidden` + `innerWidth:0` → הפריסה קורסת וכל המידות מחזירות 0.
**מדידת צבע אמינה, מדידת מיקום לא.** תמיד `resize_window` לפני מדידות פריסה.

### 🟡 `loading="lazy"` בטאב מוסתר
תמונות לא נטענות → גבהים שגויים. להסיר `loading` ולטעון מחדש לפני מדידה.

### 🟡 React מרנדר `hrefLang` כמו שהוא
לא ממיר ל-lowercase. חובה `{...{hreflang:"he"}}`.

### 🟡 עדכון state בלחיצות רצופות
סטפר שקורא `p.value` מה-closure נדרס. חובה updater פונקציונלי: `setP(s => ...)`.

### 🟡 ספציפיות CSS
`.how-ba figure { margin: 0 }` גובר על `.how-ba-after { margin-top: -18% }`.
כלל שלא עובד - בדוק ספציפיות לפני שאתה משנה ערכים.

### 🟡 צילומי מסך של עמוד מלא
כלי הצילום תופר תמונות וחותך את תחתית הדף. אל תסיק "האלמנט חסר" מצילום - בדוק DOM.

### 🟡 מזהי server functions הם SHA
לא שם קריא. לחלץ מ-`.output/server/__23tanstack-start-server-fn-resolver-*.mjs`.

---

## 7. סטטוס - רשימת הלקוח (14 סעיפים)

| # | סעיף | מצב |
|---|---|---|
| 1א | שתי מערכות סליקה | 🟢 זהה למקור. הקישורים ב-`site_settings` - החלפה = שינוי שדה |
| 1ב | כפתור תרומה בולט | ✅ כפתור צף במובייל, עמוד הבית בלבד |
| 1ג | הצהרת מס US/CA | ✅ הוסרה מהמטא (הייתה **שלנו**, לא במקור) |
| 2א | "לפני/אחרי" לא נראים | ✅ תוקן, 3 סבבים, מיושר למקור |
| 2ב | סיפורים לא נפתחים | ⚪ לא שוחזר. תוכן זהה למקור תו-מול-תו. הלקוח לא ידע לפרט |
| 2ג | גיל המייסד 50 | ✅ תוקן + ב-`site_settings` |
| 3א | סדר רבנים באנגלית | ✅ תוקן + באדמין |
| 3ב | לוגואים באנגלית | ✅ תוקן + באדמין |
| 4 | אישורי עמותה | 🟡 טבלה + אדמין + `/certificates` מוכנים. **ממתין לקבצים** |
| 5 | נגישות | ✅ הצהרה + סרגל 18 פקדים |
| 6 | חומרים עיצוביים | ✅ `/admin/media` + `/brand-kit` |
| 7 | אתר ישן / כפילות | 🔴 **פתוח.** ראה למטה |

### מעבר לרשימה (נוסף ביוזמתנו)
מדיניות פרטיות · תקנון · באנר עוגיות אוכף · פוטר עם `NGO` schema · **לידים נשמרים** ·
מערכת ניהול מלאה · hreflang · sitemap עם 83 כתובות.

### ✅ סעיף 7 - המעבר לדומיין (הושלם 25.8.26)
האתר חי על `or-hadash.org.il`. `tefilin.or-hadash.org.il` מצביע לאותה תשתית
ומגיש הפניות בלבד - הוורדפרס מנותק מהרשת ואין כפילות תוכן.

**איך זה בנוי:** לאבבל מפנה בעצמו כל דומיין משני לראשי ב-302 ששומר על הנתיב,
לפני שהבקשה מגיעה ל-`src/server.ts`. לכן כללי ההפניה חלים על **כל** בקשה,
ועל הדומיין הראשי פועלים רק כשהיעד שונה מהנתיב. המפה: `src/lib/legacy-redirects.ts`.
שרשרת בפועל: `302` (לאבבל) → `301` (שלנו) → `200`.

**שני דברים שקל ליפול בהם אם עורכים את המפה:**
- כל הכתובות הישנות נכתבו עם סלאש סוגר. הראוטר מנרמל אותו ב-307 זמני, ולכן
  אנחנו מנרמלים בעצמנו ב-301 - בלי זה 75 כתובות לא מקבלות 301 בכלל.
- באתר הישן `/letters/` הכיל **שני** סוגי תוכן: מכתבי תודה **והסכמות רבנים**.
  כלל תחילית גורף שולח 17 הסכמות לעמוד הלא נכון.

**מה שנשאר:** לבקש מוונגוס גיבוי מלא של הוורדפרס (SQL + uploads) לפני שהשרת
הישן מושבת - אין לנו גישה אליו יותר.

**גם:** לשנות את ה-`<title>` של עמוד הבית לפי בקשת הלקוח:
"קשר של תפילין - מיזם של תרומה וערבות הדדית לכל מי שרוצה להתחיל להניח תפילין" (75 תווים,
גוגל חותך ב-60 - הוצע ניסוח מקוצר, הלקוח לא הכריע).

---

## 8. 🔴 מה שלא נבדק - חובה לבדוק

**מסלול המשתמש המאושר באדמין לא נבדק בלחיצות.** הסיבה: מי שבנה לא יכול היה ליצור
סיסמה או חשבון. נבדק: כל הנתיבים עולים, הנתונים במסד נכונים, השרת דוחה בקשות לא מורשות.
**לא נבדק:** שמירה, מחיקה, שינוי סדר, העלאת קובץ.

### בדיקות חובה (10 דקות)
היכנס ל-`/admin` עם משתמש מהרשימה, ובדוק:

1. **סיפורים** - ערוך כותרת, שמור, ודא שהשתנה ב-`/stories`
2. **סדר** - חיצי ▲▼ בהסכמות רבנים, ודא שהסדר השתנה ב-`/agreements`
3. **מתג שפה** - עבור לאנגלית בכל מסך, ודא שהתוכן האנגלי מופיע ונערך
4. **מדיה** - העלה קובץ, סמן "ציבורי", ודא שהוא מופיע ב-`/brand-kit`
5. **הגדרות** - שנה טלפון, שמור, ודא שהשתנה בפוטר
6. **פניות** - מלא טופס ב-`/request`, ודא שהפנייה מופיעה ב-`/admin/leads`
7. **אישורים** - הוסף אישור, ודא שהוא מופיע ב-`/certificates`
8. **מחיקה** - נסה למחוק (רק admin רשאי)

**הצד האנגלי דורש תשומת לב מיוחדת** - שם נעשו הכי הרבה שינויים, ושם גם נשברה
הכתובת הקנונית פעם אחת.

---

## 9. מה שנשאר לעשות

| עדיפות | משימה | הערה |
|---|---|---|
| 🔴 | **בדיקות §8** | לפני שמוסרים ללקוח |
| 🔴 | **דומיין + 301 + כיבוי האתר הישן** | סעיף 7 |
| 🟡 | **התראה על ליד חדש** | וואטסאפ אוטומטי דורש ספק בתשלום (Twilio/360dialog). **Lovable Cloud כולל Emails מובנה** - זו הדרך המהירה |
| 🟡 | משתמש אדמין ללקוח | `INSERT` אחד ל-`admin_allowlist` |
| 🟢 | אישור סעיף 46 | **הלקוח ביקש לא לגעת בשום דבר שקשור לזה** |
| 🟢 | דפדוף באדמין | 59 סיפורים עובד; ב-200 יהיה איטי |
| 🟢 | מחיקה רכה | כרגע מחיקה סופית |
| 🟢 | תפקיד `editor` | המנגנון בנוי, אף אחד לא משתמש בו |

### ⚠️ שאלה פתוחה ללקוח: סעיף 46
האתר מצהיר ב-3 מקומות "התרומה מוכרת לצרכי מס", אבל הלקוח כתב "כשנקבל אישור לסעיף 46" -
כלומר ייתכן שאין לו אישור בתוקף. **בעלת הפרויקט הורתה במפורש לא לגעת בזה.** אל תשנה.

---

## 10. מפת קבצים

```
src/lib/
  site.ts                  ← SITE_URL - מקור אמת יחיד לדומיין
  settings.tsx             ← הגדרות + SETTING_DEFAULTS (נפילות)
  content.ts               ← קריאת תוכן בצד שרת
  supabase.server.ts       ← publicDb / adminDb / requireStaff
  supabase-browser.ts      ← client לדפדפן, auth בלבד
  hreflang.ts              ← מיפוי עברית↔אנגלית
  api/
    auth.functions.ts      ← config ציבורי + אימות צוות
    content.functions.ts   ← CRUD גנרי + הגדרות
    leads.functions.ts     ← שליחת טופס (ציבורי) + ניהול
    seed.functions.ts      ← הגירת תוכן מקוד ל-DB + אימות
src/components/
  admin/ContentManager.tsx ← מסך CRUD גנרי לכל סוג תוכן
  AccessibilityBar.tsx     ← סרגל נגישות, 18 פקדים
  CookieConsent.tsx        ← באנר עוגיות + אכיפה
src/routes/
  admin*.tsx               ← 12 מסכי ניהול
  en.index.tsx             ← עמוד הבית האנגלי (998 שורות, מכיל את כל הרכיבים)
  en.the-tefillin-tie-initiative.tsx  ← ⚠️ מרנדר את EnPage מ-en.index
scripts/gen-sitemap.mjs    ← מחולל סייטמאפ, קורא SITE_URL
```

---

## 11. פקודות שימושיות

```bash
bun run build                        # חייב לעבור לפני פוש
bun run dev --port 4321              # DB לא יעבוד מקומית - זה תקין
node scripts/gen-sitemap.mjs         # אחרי הוספת סיפור או מעבר דומיין
```

**בדיקת פרודקשן** (אל תסמוך על ספירה של מודל - השתמש ב-grep):
```bash
curl -s https://or-hadash.org.il/sitemap.xml | grep -c '<loc>'
curl -s https://or-hadash.org.il/ | grep -o 'מעל [0-9,]*'
```

**SQL** דרך Lovable MCP `query_database` - **תומך ב-DDL מלא**, מתחבר כ-`postgres`
(התיעוד שאומר "read-only" מיושן).

---

## 12. שלושה דברים שקל לשבור

1. **אל תסיר את הנפילות לקוד.** נראות מיותרות עד שה-DB נופל.
2. **אל תוסיף `useLoaderData` לרכיב משותף.** ראה §6.
3. **אל תריץ חיפוש-והחלפה על הדומיין.** יש `SITE_URL` אחד. 32 קבצים השתמשו בו קשיח,
   וזו בדיוק הסיבה שריכזנו.
